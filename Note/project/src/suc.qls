// 导入类型和标准库
import ql::lang::type;
import ql::std::io;

// 定义模块
mod<pub> Main {
	// 定义类
	class<pub> Tool() {
		// 属性
		attr<pub> name = "Tool类";
		
		// 方法
		op<pub, type.String> getName() {
			return name;
		}
	}
}